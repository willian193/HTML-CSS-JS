info:
  name: ADD
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3040/cursos
  body:
    type: json
    data: |-
      {
        "nome": "Willian Hetero"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
